apiVersion: 1

providers:
  - name: jwt-demo-reactive-dashboards
    orgId: 1
    folder: JWT Demo Reactive
    type: file
    disableDeletion: false
    allowUiUpdates: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json

